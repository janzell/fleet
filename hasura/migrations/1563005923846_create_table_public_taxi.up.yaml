- args:
    sql: CREATE TABLE "public"."taxi"("id" serial NOT NULL, "plate_number" text NOT
      NULL, "color" text NOT NULL, "notes" text NOT NULL, "created_at" date NOT NULL
      DEFAULT now(), "updated_at" date NOT NULL DEFAULT now(), PRIMARY KEY ("id")
      , UNIQUE ("plate_number"));
  type: run_sql
- args:
    name: taxi
    schema: public
  type: add_existing_table_or_view
