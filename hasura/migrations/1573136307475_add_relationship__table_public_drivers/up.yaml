- args:
    name: drivers_character_references
    table:
      name: drivers
      schema: public
    using:
      foreign_key_constraint_on:
        column: driver_id
        table:
          name: drivers_character_reference
          schema: public
  type: create_array_relationship
